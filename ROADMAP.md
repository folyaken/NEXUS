# План работ NEXUS

Живой список: что сделано, что в работе, что впереди.
Обновляется по ходу дела, чтобы идеи не терялись между сессиями.

---

## Сделано

### Работает у пользователей

| Возможность | Состояние |
| --- | --- |
| Обход блокировок (Zapret) | Работает, ядро запускается напрямую |
| Доступ к Telegram (TG WS Proxy) | Работает, порт и режим настраиваются |
| VPN: VLESS, VMess, Trojan, Shadowsocks, Hysteria2 | Работает |
| Подписки провайдеров | Работает, включая панели с привязкой к устройству |
| Раздельная маршрутизация по программам | Работает, выбор из списка запущенных |
| Раздача на устройства домашней сети | Работает |
| Свои сайты для обхода блокировок | Работает, список по страницам |
| Обновление одной кнопкой | Работает через публичный канал релизов |
| Запуск вместе с Windows | Работает через планировщик заданий |
| Режим TUN (весь трафик через VPN) | Работает, драйвер входит в установщик |
| Русский и английский язык | Работает, переведены оба языка целиком |
| Канал сообщества | Ссылка в «О программе» и в меню значка возле часов |

### Выпуск и распространение

- Установщик для Windows с русским интерфейсом и выбором ярлыков
- Права администратора запрашиваются автоматически
- Ядра Xray и sing-box входят в установщик
- Личные данные (ключи, профили, списки) в сборку не попадают
- Канал обновлений: `folyaken/NEXUS-releases`
- 38 наборов автоматических проверок

---

## В работе

### 1. Канал в Telegram

Сделано: ссылка на канал внутри программы, файл `CHANGELOG.md` и команда
`npm run post` — она собирает готовый текст поста о новой версии.
План работы с каналом целиком описан в `TELEGRAM.md`.

Дальше по желанию: свой бот с командой `/download` и автопубликация поста при
создании релиза на GitHub.

### 2. Уведомление об обновлении

Сделано: проверка при запуске (через 8 секунд, в фоне) и точка рядом с пунктом
«О программе».

Можно развить: показывать номер новой версии при наведении, добавить строку в
меню трея.

---

## Впереди

### Ближайшее

- **Подпись установщика.** Windows показывает предупреждение при запуске.
  Сертификат стоит 300–400 $ в год. Пока обходится кнопкой «Выполнить в любом
  случае».
- **Проверка антивирусами.** Отправить сборку в Microsoft на исключение:
  модуль обхода блокировок использует сетевой драйвер, антивирусы к такому
  относятся настороженно.

### Идеи на потом

**Присмотреться к решениям других программ (Browsec, Happ).** Отдельно
обсудить, что именно оттуда стоит взять — подход к интерфейсу, способ выбора
серверов, показ скорости и нагрузки, работа в трее.

Возможные направления:
- Расширение для браузера или отдельный режим только для браузера
- Быстрое переключение серверов из трея, без открытия окна
- График скорости и объёма трафика
- Проверка доступности сервера перед подключением

### Технический долг

- Перенос проекта в чистый репозиторий (планировалось изначально)
- Проверка на слабом железе и при медленной сети

---

## Как выпускается новая версия

1. Дописать раздел версии в `CHANGELOG.md` — человеческим языком
2. Поднять `version` в `package.json`
3. `npm test` — все наборы должны пройти
4. `npm run release:win`
5. Создать релиз в `NEXUS-releases`, приложить три файла:
   установщик, `latest.yml`, `.blockmap`
6. `npm run post` — готовый текст для канала

Подробности — в `UPDATES.md`.
