/**
 * Заглушка подписи кода.
 *
 * Сертификата подписи у проекта пока нет (пункт 8 чеклиста релиза), но
 * electron-builder всё равно пытается подписать каждый .exe: сначала скачивает
 * пакет winCodeSign, затем распаковывает его. В архиве есть символические
 * ссылки для macOS, и без прав на их создание Windows выдаёт поток ошибок
 * «Cannot create symbolic link … libcrypto.dylib» — на каждый исполняемый файл.
 *
 * Штатный способ отключить это — задать собственную функцию подписи. Она просто
 * ничего не делает, поэтому winCodeSign не скачивается вовсе.
 *
 * Важно: правка ресурсов при этом сохраняется. Именно она записывает в манифест
 * requestedExecutionLevel — запрос прав администратора, без которого не работают
 * Zapret и режим TUN. Флаг signAndEditExecutable: false отключил бы и её.
 *
 * Когда появится сертификат, этот файл нужно убрать из package.json и настроить
 * подпись штатным образом.
 */
exports.default = async function noSign() {
  // Подпись намеренно пропускается: сертификат не настроен.
};
